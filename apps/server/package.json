{
  "name": "@chessss/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@chessss/chess-core": "workspace:*",
    "@chessss/shared": "workspace:*",
    "fastify": "^5.4.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "tsx": "^4.20.3",
    "vitest": "^3.2.4"
  }
}
